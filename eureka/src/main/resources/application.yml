eureka:
  instance:
    hostname: eureka1
  client:
    service-url:
      defaultZone: http://eureka:8762/eureka/
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka
